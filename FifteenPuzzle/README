Extra Features:
I created app icons for all the different sizes (except for app store).
I used images for buttons.
I make the shuffle button into a solve button as well
I made the image button into a numbers button, so user can alternate
For the victory, the whole screen does a barrel roll.
